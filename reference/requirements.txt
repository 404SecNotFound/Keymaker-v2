# Dependencies for the independent KEYM v1 reference implementation.
#
# Pinned exactly, not floated with >=. A conformance oracle whose own crypto
# libraries drift between runs cannot distinguish "the implementations
# disagree" from "the oracle changed underneath us". Dependabot proposes
# upgrades explicitly; nothing moves implicitly.
cryptography==41.0.7
argon2-cffi==25.1.0
